const LIVE_URL = ' https://kokoranch4.herokuapp.com'
const LOCAL_URL = 'http://localhost:3009'

const BASE_URL = LIVE_URL
const TOKEN = localStorage.getItem('token')

export { LIVE_URL, LOCAL_URL, TOKEN, BASE_URL }
